<div class="container">
  <h1>Welcome to Sinatra!</h1>
  <p>Hello, world!</p>

  <table>
    <tr>
      <th>File Description</th>
      <th>File Location</th>
    </tr>
    <tr>
      <td>This view file</td>
      <td><code><%= __FILE__.gsub(Sinatra::Application.root, '') %></code></td>
    </tr>
    <tr>
      <td>The layout file</td>
      <td><code><%= File.expand_path('../layout.erb', __FILE__).gsub(Sinatra::Application.root, '') %></code></td>
    </tr>
    <tr>
      <td>The current controller</td>
      <td><code><%= File.expand_path('../../controllers/index.rb', __FILE__).gsub(Sinatra::Application.root, '') %></code></td>
    </tr>
  </table>

  <h4>Create Toy?</h4>

  <form action="/sandbox" method="get">
    <select name="select">
      <option value="Robbie the Robot Toy">Robbie the Robot</option>
      <option value="Charlie's Angels" selected>Charlie's Angels</option>
      <option value="Indiana Jones">Indiana Jones</option>
    </select>
    <input type="submit">
  </form>

  <form action="/sandbox" method="post">
    <input type="text" name="toy">
    <input type="text" name="post[title]">
    <input type="text" name="post[price]">
    <textarea name="post[description]"></textarea>
    <input type="submit">
  </form>

</div>
